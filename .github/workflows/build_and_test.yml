name: Build and Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  GODOT_VERSION: "4.5"
  GODOT_VERSION_FULL: "4.5.0"
  SCONS_CACHE_LIMIT: 7168

jobs:
  # Build and test on Linux only
  build-and-test-linux:
    runs-on: ubuntu-latest
    name: Build and Test (Linux x86_64)

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup GDExtension Build Environment
        uses: NodotProject/godot-cpp-builds@v1
        with:
          godot-version: '4.5-stable'
          godot-cpp-path: './godot-cpp'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          echo "set man-db/auto-update false" | sudo debconf-communicate
          sudo dpkg-reconfigure man-db
          sudo apt-get update
          sudo apt-get install -y build-essential scons pkg-config libdbus-1-dev \
            libbz2-dev libx11-dev libxcursor-dev libxinerama-dev \
            libgl1-mesa-dev libglu1-mesa-dev libasound2-dev libpulse-dev \
            libudev-dev libxi-dev libxrandr-dev

      - name: Setup SCons cache
        uses: actions/cache@v4
        with:
          path: |
            ${{ github.workspace }}/.scons-cache/
          key: linux-test-scons-${{ hashFiles('**/*.cpp', '**/*.h') }}
          restore-keys: |
            linux-test-scons-

      - name: Build GDExtension
        run: |
          scons platform=linux target=template_release -j$(nproc)
        env:
          SCONS_CACHE: ${{ github.workspace }}/.scons-cache/

      - name: Verify build output
        run: |
          ls -lh addons/godot-binary-patcher/bin/
          file addons/godot-binary-patcher/bin/libgodot_binary_patcher.so

      - name: Install Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: ${{ env.GODOT_VERSION_FULL }}
          use-dotnet: false
          include-templates: false

      - name: Import project
        run: |
          timeout 60 godot --headless --import --quit || true

      - name: Create user data directory and dummy files for tests
        run: |
          mkdir -p ~/.local/share/godot/app_userdata/godot-binary-patcher
          touch ~/.local/share/godot/app_userdata/godot-binary-patcher/old_file_test.bin
          touch ~/.local/share/godot/app_userdata/godot-binary-patcher/new_file_expected.bin

      - name: Run GUT tests
        run: |
          godot --headless --verbose -s addons/gut/gut_cmdln.gd -gdir=res://test/unit -gexit

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            .gut/
            *.log
          retention-days: 7

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: libgodot_binary_patcher-linux-x86_64
          path: addons/godot-binary-patcher/bin/libgodot_binary_patcher.so
          retention-days: 7
